xford pane utility controls */
.transcript-heading{position:sticky;top:0;z-index:6;background:var(--paper-hi);align-items:center}
.transcript-heading-actions{display:flex;align-items:center;gap:7px}
.jump-latest,.pane-focus{
  min-height:24px;
  padding:0 7px;
  border:1px solid var(--rule-soft);
  border-radius:2px;
  background:var(--paper-hi);
  color:var(--muted);
  cursor:pointer;
  font:600 6.5px/1 var(--mono);
  letter-spacing:.025em;
}
.jump-latest:hover,.pane-focus:hover{border-color:var(--verm);color:var(--verm)}
.pane-focus{width:26px;padding:0;font-size:11px}
.board-appbar-actions{display:flex;align-items:center;gap:6px}
.board-appbar .pane-focus{background:rgba(255,255,255,.72)}
body[data-theme="dark"] .board-appbar .pane-focus{background:#383835}

/* Pane focus states */
.live-main[data-focus="transcript"]{
  grid-template-columns:minmax(0,1fr) 0 0!important;
}
.live-main[data-focus="transcript"] .split-divider,
.live-main[data-focus="transcript"] .board-pane{display:none}
.live-main[data-focus="board"]{
  grid-template-columns:0 0 minmax(0,1fr)!important;
}
.live-main[data-focus="board"] .split-divider,
.live-main[data-focus="board"] .reasoning-pane{display:none}
.live-main[data-focus] .pane-focus[data-focus-pane]{border-color:var(--rule-soft)}
.live-main[data-focus="transcript"] .pane-focus[data-focus-pane="transcript"],
.live-main[data-focus="board"] .pane-focus[data-focus-pane="board"]{
  border-color:var(--verm);
  color:var(--verm);
  background:var(--verm-soft);
}

/* End interview popover */
.end-control{position:relative}
.end-popover{
  position:absolute;
  z-index:80;
  top:calc(100% + 8px);
  right:0;
  width:250px;
  padding:13px;
  border:1px solid var(--rule);
  border-radius:3px;
  background:var(--paper-hi);
  box-shadow:0 18px 46px rgba(34,26,17,.18);
  color:var(--ink);
  opacity:0;
  visibility:hidden;
  transform:translateY(-4px);
  transition:opacity .12s ease,transform .12s ease,visibility .12s;
}
.end-control.open .end-popover{opacity:1;visibility:visible;transform:translateY(0)}
.end-popover strong{font:600 13px/1.3 var(--serif)}
.end-popover p{margin:5px 0 12px;font:400 9px/1.45 var(--sans);color:var(--muted)}
.end-popover>div{display:flex;justify-content:flex-end;gap:6px}
.end-popover button{min-height:28px;padding:0 9px;border-radius:2px;cursor:pointer;font:600 8px/1 var(--sans)}
.popover-cancel{border:1px solid var(--rule);background:transparent;color:var(--ink-2)}
.popover-confirm{border:1px solid var(--verm);background:var(--verm);color:#fff}
body[data-theme="dark"] .end-popover{background:#38342f;box-shadow:0 20px 48px rgba(0,0,0,.42)}

/* Scroll-edge cues: only appear when JS reports hidden content above/below. */
.scroll-cue{
  --cue-top:transparent;
  --cue-bottom:transparent;
  box-shadow:
    inset 0 14px 13px -14px var(--cue-top),
    inset 0 -14px 13px -14px var(--cue-bottom);
}
.scroll-cue[data-at-top="false"]{--cue-top:rgba(45,37,28,.28)}
.scroll-cue[data-at-bottom="false"]{--cue-bottom:rgba(45,37,28,.28)}
body[data-theme="dark"] .scroll-cue[data-at-top="false"]{--cue-top:rgba(0,0,0,.5)}
body[data-theme="dark"] .scroll-cue[data-at-bottom="false"]{--cue-bottom:rgba(0,0,0,.5)}
.custom-select-menu.scroll-cue{max-height:340px;overflow-y:auto}

/* Tiny prototype toast */
.prototype-toast{
  position:fixed;
  z-index:120;
  right:18px;
  bottom:18px;
  max-width:300px;
  padding:9px 11px;
  border:1px solid var(--rule);
  border-radius:3px;
  background:var(--paper-hi);
  color:var(--ink-2);
  box-shadow:0 12px 34px rgba(31,24,16,.15);
  font:500 8px/1.35 var(--sans);
  opacity:0;
  pointer-events:none;
  transform:translateY(5px);
  transition:opacity .14s ease,transform .14s ease;
}
.prototype-toast.show{opacity:1;transform:translateY(0)}
body[data-theme="dark"] .prototype-toast{background:#38342f;box-shadow:0 16px 38px rgba(0,0,0,.42)}

@media(max-width:760px){
  .pane-focus{display:none}
  .jump-latest{display:none!important}
}


/* ================================================================
   V9 — background-only homepage treatment map
   ================================================================ */

.home-hero{
  position:relative;
  isolation:isolate;
  overflow:hidden;
}
.hero-copy,
.folio{
  position:relative;
  z-index:2;
}
.hero-bg-switcher{
  position:absolute;
  top:18px;
  right:6px;
  z-index:6;
  display:flex;
  align-items:center;
  gap:5px;
  padding:4px 5px;
  background:color-mix(in srgb,var(--paper) 82%,transparent);
  backdrop-filter:blur(2px);
}
.hero-bg-switcher>span{
  margin-right:7px;
  font:600 6px/1 var(--mono);
  letter-spacing:.11em;
  color:var(--faint);
}
.hero-bg-switcher button{
  width:22px;
  height:22px;
  padding:0;
  border:1px solid var(--rule-soft);
  border-radius:999px;
  background:transparent;
  color:var(--muted);
  font:600 7px/1 var(--mono);
}
.hero-bg-switcher button:hover{
  border-color:var(--rule);
  color:var(--ink);
}
.hero-bg-switcher button.active{
  border-color:var(--verm);
  background:var(--verm-soft);
  color:var(--verm);
}

.hero-bg-layer{
  position:absolute;
  z-index:0;
  inset:0;
  opacity:0;
  pointer-events:none;
  transition:opacity .16s ease;
}
.hero-bg-layer.active{opacity:1}

.bg-treatment-legend{
  display:flex;
  flex-wrap:wrap;
  gap:7px 16px;
  padding:9px 4px 11px;
  border-bottom:1px solid var(--rule-faint);
  color:var(--faint);
  font:500 6.5px/1 var(--mono);
  letter-spacing:.03em;
}
.bg-treatment-legend b{
  color:var(--verm);
  font-weight:650;
  margin-right:3px;
}

/* 1 — oversized ghost typography */
.hero-bg-ghost{
  overflow:hidden;
}
.ghost-type{
  position:absolute;
  user-select:none;
  color:var(--ink);
  opacity:.027;
  font-family:var(--serif);
  letter-spacing:-.07em;
  white-space:nowrap;
}
.ghost-type-a{
  top:-95px;
  right:-42px;
  font-size:430px;
  line-height:1;
}
.ghost-type-b{
  right:-210px;
  bottom:-35px;
  font:600 112px/1 var(--mono);
  letter-spacing:.08em;
  transform:rotate(-90deg);
  transform-origin:center;
  opacity:.018;
}

/* 2 — sparse mathematical marginalia */
.hero-bg-marginalia svg,
.hero-bg-circle svg{
  width:100%;
  height:100%;
}
.bg-pencil{
  fill:none;
  stroke:var(--ink);
  stroke-width:1.15;
  stroke-linecap:round;
  opacity:.065;
}
.bg-pencil.dashed{
  stroke-dasharray:7 9;
  opacity:.048;
}
.bg-pencil-circle{
  fill:none;
  stroke:var(--ink);
  stroke-width:1;
  opacity:.055;
}
.bg-red-dot{
  fill:var(--verm);
  opacity:.46;
}
.bg-mini-label{
  fill:var(--ink);
  font:italic 18px/1 var(--serif);
  opacity:.085;
}

/* 3 — print crop / registration marks */
.crop-mark{
  position:absolute;
  width:46px;
  height:46px;
  opacity:.12;
}
.crop-mark::before,
.crop-mark::after{
  content:"";
  position:absolute;
  background:var(--ink);
}
.crop-mark::before{width:30px;height:1px}
.crop-mark::after{width:1px;height:30px}
.crop-a{top:70px;right:92px}
.crop-a::before{top:0;right:0}
.crop-a::after{top:0;right:0}
.crop-b{right:28px;bottom:82px;transform:rotate(180deg)}
.crop-c{top:145px;right:500px;transform:rotate(90deg);opacity:.055}
.crop-d{right:350px;bottom:48px;transform:rotate(-90deg);opacity:.055}
.registration-cross{
  position:absolute;
  top:226px;
  right:176px;
  width:26px;
  height:26px;
  opacity:.1;
}
.registration-cross::before,
.registration-cross::after{
  content:"";
  position:absolute;
  left:50%;
  top:50%;
  background:var(--ink);
  transform:translate(-50%,-50%);
}
.registration-cross::before{width:26px;height:1px}
.registration-cross::after{width:1px;height:26px}
.registration-dot{
  position:absolute;
  top:236px;
  right:186px;
  width:6px;
  height:6px;
  border-radius:50%;
  background:var(--verm);
  opacity:.52;
}

/* 4 — partial ruled-paper zones */
.ruled-zone{
  position:absolute;
  background:repeating-linear-gradient(
    to bottom,
    transparent 0,
    transparent 31px,
    color-mix(in srgb,var(--ink) 7%,transparent) 31px,
    color-mix(in srgb,var(--ink) 7%,transparent) 32px
  );
  mask-image:linear-gradient(to right,transparent,#000 14%,#000 82%,transparent);
  opacity:.42;
}
.ruled-zone-a{
  top:74px;
  right:18px;
  width:520px;
  height:214px;
}
.ruled-zone-b{
  right:250px;
  bottom:26px;
  width:360px;
  height:130px;
  opacity:.22;
}

/* 5 — large compass trace */
.bg-compass{
  fill:none;
  stroke:var(--verm);
  stroke-width:1.15;
  opacity:.055;
}

/* 6 — vertical page index */
.vertical-index{
  position:absolute;
  top:86px;
  right:22px;
  display:flex;
  gap:22px;
  writing-mode:vertical-rl;
  text-orientation:mixed;
}
.vertical-index span{
  color:var(--ink);
  opacity:.09;
  font:600 6.5px/1 var(--mono);
  letter-spacing:.11em;
}
.index-tick{
  position:absolute;
  right:80px;
  width:1px;
  background:var(--ink);
  opacity:.08;
}
.index-tick-a{top:116px;height:72px}
.index-tick-b{bottom:92px;height:116px}

/* 7 — grid fragment */
.grid-fragment{
  position:absolute;
  right:54px;
  bottom:28px;
  width:510px;
  height:320px;
  opacity:.29;
  background-image:
    linear-gradient(to right,color-mix(in srgb,var(--ink) 6%,transparent) 1px,transparent 1px),
    linear-gradient(to bottom,color-mix(in srgb,var(--ink) 6%,transparent) 1px,transparent 1px);
  background-size:42px 42px;
  mask-image:radial-gradient(ellipse at center,#000 10%,rgba(0,0,0,.85) 36%,transparent 76%);
}
.grid-point{
  position:absolute;
  right:286px;
  bottom:170px;
  width:7px;
  height:7px;
  border-radius:50%;
  background:var(--verm);
  opacity:.48;
}
.grid-label{
  position:absolute;
  right:270px;
  bottom:176px;
  color:var(--ink);
  opacity:.095;
  font:italic 16px/1 var(--serif);
}

/* 8 — embossed watermark */
.watermark-mark{
  position:absolute;
  top:76px;
  right:78px;
  width:330px;
  height:330px;
  color:var(--ink);
  opacity:.024;
  transform:rotate(-6deg);
}
.watermark-caption{
  position:absolute;
  top:373px;
  right:94px;
  color:var(--ink);
  opacity:.022;
  font:600 46px/1 var(--mono);
  letter-spacing:.14em;
}

/* Dark-mode background treatments remain extremely quiet. */
body[data-theme="dark"] .hero-bg-switcher{
  background:rgba(43,41,38,.82);
}
body[data-theme="dark"] .ghost-type,
body[data-theme="dark"] .vertical-index span,
body[data-theme="dark"] .watermark-caption,
body[data-theme="dark"] .watermark-mark{
  color:#fff;
}
body[data-theme="dark"] .bg-pencil,
body[data-theme="dark"] .bg-pencil-circle,
body[data-theme="dark"] .crop-mark::before,
body[data-theme="dark"] .crop-mark::after,
body[data-theme="dark"] .registration-cross::before,
body[data-theme="dark"] .registration-cross::after,
body[data-theme="dark"] .index-tick{
  stroke:#fff;
  background:#fff;
}
body[data-theme="dark"] .bg-mini-label,
body[data-theme="dark"] .grid-label{
  fill:#fff;
  color:#fff;
}
body[data-theme="dark"] .ruled-zone{
  opacity:.25;
}
body[data-theme="dark"] .grid-fragment{
  opacity:.22;
}

@media(max-width:1180px){
  .hero-bg-switcher{
    top:8px;
    right:2px;
  }
  .bg-treatment-legend{
    display:none;
  }
}
@media(max-width:760px){
  .hero-bg-switcher{
    position:relative;
    top:auto;
    right:auto;
    width:max-content;
    margin-bottom:8px;
  }
  .hero-bg-layer{
    display:none;
  }
}


/* ================================================================
   V10 — stronger sidebar, centered hero copy, more visible bg accents
   ================================================================ */

/* Sidebar refresh: use a quiet “index card / tab” language instead of plain rows. */
.side-rail{
  padding:18px 14px 16px;
  background:
    linear-gradient(to bottom, color-mix(in srgb,var(--paper-hi) 78%,var(--paper)) 0%, color-mix(in srgb,var(--paper-hi) 68%,var(--paper)) 100%);
}
.side-brand{
  padding:9px 9px 11px;
  border:1px solid var(--rule-faint);
  border-radius:3px;
  background:color-mix(in srgb,var(--paper-hi) 56%,transparent);
}
.side-brand .brand-lockup{
  grid-template-columns:30px minmax(0,1fr);
  gap:10px;
}
.side-brand .mark{
  width:24px;
  height:24px;
}
.side-brand strong{
  font:500 17px/1 var(--serif);
  letter-spacing: