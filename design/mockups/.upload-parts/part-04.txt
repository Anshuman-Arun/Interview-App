x)}
.hero-dek{max-width:520px;margin:26px 0 0 clamp(70px,6vw,170px);font-size:clamp(14px,1vw,18px)}
.hero-actions{margin-left:clamp(70px,6vw,170px);margin-top:27px}.copy-picker{display:flex;align-items:center;gap:3px;margin:17px 0 0 clamp(70px,6vw,170px);width:max-content}.copy-picker>span{margin-right:6px;font:600 6.5px/1 var(--mono);letter-spacing:.1em;color:var(--faint)}.copy-picker button{width:24px;height:22px;border:1px solid transparent;background:transparent;border-radius:2px;font:500 7px/1 var(--mono);color:var(--muted)}.copy-picker button:hover{border-color:var(--rule)}.copy-picker button.active{background:var(--ink);color:var(--paper-hi)}
.folio{padding:0}.folio-sheet{width:min(100%,520px);aspect-ratio:.78;padding:28px;transform:rotate(.6deg)}.folio-board-v3{top:70px;bottom:88px;left:30px;right:30px}.folio-cap{bottom:23px}.folio-cap strong{font-size:17px}.folio-tag,.folio-note{display:none!important}
.modes{padding-top:29px;padding-bottom:28px}.section-head{margin-bottom:11px}.recent-v3{display:block;padding-top:28px}.recent-v3 .recent-row{grid-template-columns:28px minmax(260px,1fr) 120px 125px 62px}.recent-row small{display:none}
@media(min-width:2200px){.home-wrap{max-width:2100px}.home-hero{grid-template-columns:minmax(760px,1.15fr) minmax(500px,.62fr);gap:180px}.folio-sheet{width:560px}.hero-title{font-size:126px}.modes,.recent-v3{max-width:1880px}}
@media(max-width:1200px){.home-hero{grid-template-columns:1fr;min-height:auto}.folio{max-width:560px;margin:20px auto 0}.hero-dek,.hero-actions,.copy-picker{margin-left:0}.hero-title em{margin-left:8vw}.provider-card-v3{margin-left:0}}

/* make a few codes/version rows visually secondary or disappear entirely */
.session-line code{font-family:var(--sans);font-size:8px}.quant-head p:empty{display:none}.history-stat code{font-family:var(--sans);font-size:8px;color:var(--muted)}


.summary-copy,.history-band header>span,.transcript-heading span:last-child{display:none!important}

/* V4 refinements — zoom-stable hero, tighter setup, richer model catalog */
.side-rail{
  position:relative;
  overflow:hidden;
  padding:18px 13px 14px;
  background:color-mix(in srgb,var(--paper-hi) 72%,var(--paper));
}
.side-rail:before{
  content:"";
  position:absolute;
  top:0;
  left:37px;
  width:1px;
  height:100%;
  background:linear-gradient(to bottom,transparent 0 64px,var(--rule-faint) 64px calc(100% - 86px),transparent calc(100% - 86px));
  pointer-events:none;
}
.side-brand{
  position:relative;
  z-index:1;
  padding:4px 5px 12px;
}
.side-brand:after{
  content:"";
  position:absolute;
  left:5px;
  bottom:0;
  width:38px;
  height:1px;
  background:var(--verm);
}
.new-cta{
  position:relative;
  z-index:1;
  margin-top:21px;
  box-shadow:4px 4px 0 var(--rule-faint);
}
.side-nav{
  position:relative;
  z-index:1;
  margin:11px 0 0;
  padding:5px 0;
  border-top:1px solid var(--rule-faint);
  border-bottom:1px solid var(--rule-faint);
}
.side-nav button{
  margin:1px 0;
  border-radius:2px;
}
.side-nav button.active{
  background:color-mix(in srgb,var(--paper-hi) 70%,transparent);
}
.side-runtime{
  position:relative;
  z-index:1;
  margin:0 1px 1px;
  padding:11px 10px 9px;
  border:1px solid var(--rule-faint);
  background:color-mix(in srgb,var(--paper-hi) 48%,transparent);
}
.side-runtime .runtime-meta{
  grid-template-columns:1fr auto;
  row-gap:5px;
}
.side-runtime .runtime-meta b{color:var(--green)}

.home-hero{
  grid-template-columns:minmax(680px,1.15fr) minmax(440px,.66fr);
  gap:clamp(70px,5.7vw,155px);
}
.hero-copy{container-type:inline-size}
.hero-title{
  display:grid;
  grid-template-columns:minmax(0,1fr);
  width:min(100%,1100px);
  max-width:none;
  margin-top:25px;
  font-size:clamp(74px,5.05vw,128px);
  line-height:.86;
}
.hero-title em{
  width:100%;
  margin-left:0!important;
  padding-left:0;
  text-align:center;
  color:var(--ink-2);
}
.hero-dek,.hero-actions,.copy-picker{
  margin-left:clamp(54px,9%,138px);
}
.folio-sheet{
  overflow:hidden;
  color:var(--ink);
}
.sketch{stroke:var(--ink)}
.folio-board-v3{
  top:66px;
  bottom:90px;
}
.folio-board-v3 svg{overflow:visible}
.mode-card{
  min-height:112px;
  padding:13px 17px 12px!important;
}
.mode-card h3{margin:5px 0 0}
.mode-card p{display:none}
.mode-card footer{bottom:12px}
.mode-card .num{font-size:6.5px}

.new-layout{
  grid-template-columns:minmax(0,1fr) 305px;
  gap:48px;
  max-width:1260px;
  padding-top:18px;
}
.config-section{padding:17px 0 21px}
.mode-choice{
  margin:13px 0 0 46px;
  gap:0;
  border:1px solid var(--rule);
  background:var(--paper-hi);
}
.choice-card{
  min-height:62px;
  padding:9px 14px 8px;
  border:0;
  border-right:1px solid var(--rule-soft);
  background:transparent;
  box-shadow:none;
  display:flex;
  flex-direction:column;
  justify-content:flex-start;
}
.choice-card:last-child{border-right:0}
.choice-card:hover{
  border-color:var(--rule-soft);
  background:var(--rule-faint);
}
.choice-card.selected{
  border-color:var(--rule-soft);
  background:var(--verm-soft);
  box-shadow:inset 0 -2px 0 var(--verm);
}
.choice-card strong{margin-top:5px;font-size:15px}
.choice-card .choice-tag{font-size:6.3px}

.interview-basics{
  display:grid;
  grid-template-columns:minmax(0,1fr) 150px;
  margin:8px 0 0 46px;
  border:1px solid var(--rule);
  background:var(--paper-hi);
}
.interview-basics>.field{
  min-width:0;
  padding:9px 12px 8px;
}
.interview-basics>.field:first-child{border-right:1px solid var(--rule-soft)}
.interview-basics .field>span{margin-bottom:0}
.interview-basics .custom-select-trigger{
  min-height:31px;
  padding:0;
  border:0;
  background:transparent;
}
.duration-input{
  display:flex;
  min-height:31px;
  align-items:center;
  justify-content:space-between;
  gap:8px;
}
.duration-input input{
  width:100%;
  height:31px;
  padding:0;
  border:0;
  background:transparent;
}
.duration-input small{
  font:500 7px/1 var(--mono);
  color:var(--faint);
}
.provider-card-v3{
  margin:13px 0 0 46px;
  overflow:visible;
}
.provider-card-v3 header{
  padding:10px 12px;
}
.provider-card-v3 header>div small{
  display:block;
  margin-top:2px;
  font:400 7px/1.25 var(--sans);
  color:var(--muted);
}
.provider-main-v3{padding:11px 12px 12px}
.provider-main-v3 .field{max-width:none}
.model-select .custom-select-trigger{min-height:58px}
.model-trigger-copy{min-width:0;display:grid;gap:4px}
.model-name-row{display:flex;align-items:center;gap:8px;min-width:0}
.model-name-row strong{overflow:hidden;text-overflow:ellipsis;white-space:nowrap}
.model-name-row em,.model-option-head em{
  padding:3px 5px;
  border:1px solid color-mix(in srgb,var(--verm) 35%,var(--rule));
  background:var(--verm-soft);
  color:var(--verm);
  font:600 6px/1 var(--mono);
  font-style:normal;
  letter-spacing:.05em;
}
.model-name-row .new-badge,.model-option-head .new-badge{
  border-color:color-mix(in srgb,var(--indigo) 35%,var(--rule));
  background:color-mix(in srgb,var(--indigo) 10%,var(--paper-hi));
  color:var(--indigo);
}
.model-selected-facts{
  display:grid;
  grid-template-columns:repeat(4,minmax(0,1fr));
  gap:0;
  margin-top:7px;
  border:1px solid var(--rule-faint);
}
.model-selected-facts span{
  min-width:0;
  padding:7px 8px;
  border-right:1px solid var(--rule-faint);
  font:500 7px/1.35 var(--sans);
  color:var(--ink-2);
}
.model-selected-facts span:last-child{border-right:0}
.model-selected-facts b{
  display:block;
  margin-bottom:3px;
  font:600 6px/1 var(--mono);
  letter-spacing:.05em;
  color:var(--faint);
}
.model-select .model-catalog{
  width:min(770px,calc(100vw - 300px));
  right:auto;
  padding:0;
  overflow:hidden;
}
.model-menu-label{
  padding:8px 10px 7px;
  border-bottom:1px solid var(--rule-faint);
  background:color-mix(in srgb,var(--paper-lo) 42%,var(--paper-hi));
  font:600 6.5px/1 var(--mono);
  letter-spacing:.08em;
  color:var(--verm);
}
.model-menu-label.muted-label{color:var(--faint)}
.model-catalog .model-option{
  display:block;
  min-height:0;
  padding:11px 10px 10px;
  border-bottom:1px solid var(--rule-faint);
  border-radius:0;
}
.model-option-head{
  display:flex;
  align-items:center;
  justify-content:space-between;
  gap:14px;
}
.model-option-head>span{display:flex;align-items:center;gap:5px;flex-wrap:wrap;justify-content:flex-end}
.model-option-head i{
  font:600 6px/1 var(--mono);
  font-style:normal;
  letter-spacing:.04em;
  color:var(--green);
}
.model-option.unavailable{opacity:.58;cursor:default}
.model-option.unavailable:hover{background:transparent;color:var(--ink-2)}
.model-option.unavailable .model-option-head i{color:var(--muted)}
.model-metrics{
  display:grid;
  grid-template-columns:repeat(6,minmax(0,1fr));
  gap:8px;
  margin-top:9px;
}
.model-metrics>span{
  min-width:0;
  font:500 7px/1.25 var(--sans);
  color:var(--ink-2);
}
.model-metrics b{
  display:block;
  margin-bottom:3px;
  font:600 5.8px/1 var(--mono);
  color:var(--faint);
  text-transform:uppercase;
}
.model-source-note{
  padding:8px 10px 9px;
  font:400 6.6px/1.45 var(--sans);
  color:var(--muted);
  background:color-mix(in srgb,var(--paper-lo) 28%,var(--paper-hi));
}

.session-slip{top:17px}
.session-slip h3{margin-top:22px}
.slip-list{margin-top:22px}

/* Dark mode: warmer charcoal, not near-black brown. */
body[data-theme="dark"]{
  --paper:#2b2926;
  --paper-hi:#34312d;
  --paper-lo:#242220;
  --canvas:#2b2b28;
  --ink:#f0ebe3;
  --ink-2:#d2c9bf;
  --muted:#aaa096;
  --faint:#81786f;
  --rule:#504941;
  --rule-soft:rgba(240,235,227,.14);
  --rule-faint:rgba(240,235,227,.075);
  --verm:#d87962;
  --verm-soft:#442d27;
  --indigo:#93a2bc;
  --green:#93aa8d;
  --amber:#cca061;
  --danger:#dd817c;
  background:#24221f;
}
body[data-theme="dark"] .app{
  background:
    radial-gradient(circle at 15% 2%,rgba(255,255,255,.045),transparent 29%),
    radial-gradient(circle at 85% 82%,rgba(147,162,188,.025),transparent 34%),
    repeating-linear-gradient(0deg,rgba(255,255,255,.008) 0,rgba(255,255,255,.008) 1px,transparent 1px,transparent 4px),
    var(--paper);
}
body[data-theme="dark"] .side-rail,
body[data-theme="dark"] .product-top{background:rgba(45,41,37,.86)}
body[data-theme="dark"] .hero-title em{color:var(--ink-2)}
body[data-theme="dark"] .hero-overline:before{background:var(--ink)}
body[data-theme="dark"] .folio-sheet{background:#34302b;color:var(--ink)}
body[data-theme="dark"] .folio-sheet:after{border-color:rgba(255,255,255,.045)}
body[data-theme="dark"] .sketch{stroke:var(--ink)}
body[data-theme="dark"] .choice-card,
body[data-theme="dark"] .mode-choice,
body[data-theme="dark"] .interview-basics,
body[data-theme="dark"] .provider-card,
body[data-theme="dark"] .session-slip{background:var(--paper-hi)}
body[data-theme="dark"] .choice-card.selected{background:var(--verm-soft)}
body[data-theme="dark"] .custom-select-trigger,
body[data-theme="dark"] .custom-select-menu,
body[data-theme="dark"] .interview-basics input{
  background:transparent;
  color:var(--ink);
}
body[data-theme="dark"] .model-catalog{background:var(--paper-hi)}
body[data-theme="dark"] .new-cta,
body[data-theme="dark"] .hero-primary,
body[data-theme="dark"] .start-config,
body[data-theme="dark"] .segment.selected,
body[data-theme="dark"] .qbtn.primary,
body[data-theme="dark"] .replay-filter button.selected,
body[data-theme="dark"] .setting-segments button.selected{
  background:#ece5dc;
  color:#211e1b;
  border-color:#ece5dc;
}
body[data-theme="dark"] .prototype-switcher button.active{
  background:#ece5dc;
  color:#211e1b;
}
body[data-theme="dark"] .live-shell{background:#252320}
body[data-theme="dark"] .live-top,
body[data-theme="dark"] .reasoning-pane{background:#34312d}
body[data-theme="dark"] .input-dock{background:#302d29}
body[data-theme="dark"] .board-pane{background:#292927}
body[data-theme="dark"] .board-appbar{background:#32322f;border-color:#4a4944}
body[data-theme="dark"] .tldraw-wrap{background:#262624}
body[data-theme="dark"] .tldraw-canvas{background:#2e2e2b;border-color:#4a4944}
body[data-theme="dark"] .mark .bracket{stroke:va